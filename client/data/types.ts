export type Space = { id: string; owner_id: string; name: string; kind: 'personal' | 'household' | 'work' | 'project'; colour: string; icon: string; short_label: string; is_default: boolean; protected: boolean; archived_at: string | null; space_members?: Pick<Member, 'user_id' | 'role'>[] };
export type Member = { id: string; space_id: string; user_id: string; role: 'owner' | 'admin' | 'member' | 'viewer' | 'free_busy'; status: string; profiles?: Profile | null };
export type Profile = { id: string; email: string; display_name: string; avatar_url: string | null; timezone: string; locale: string; week_starts_on: number; theme: 'system' | 'light' | 'dark'; default_space_id: string | null };
export type Task = { id: string; space_id: string; owner_id: string; category_id: string | null; recurrence_rule_id: string | null; recurrence_rules?: RecurrenceRule | null; title: string; body_md: string; status: 'todo' | 'doing' | 'blocked' | 'done' | 'dropped'; priority: 'none' | 'low' | 'normal' | 'high' | 'urgent'; visibility: 'space' | 'private'; is_locked: boolean; due_on: string | null; due_at: string | null; deferred_until: string | null; assignee_id: string | null; waiting_on: string | null; completed_at: string | null; updated_at: string };
export type ChecklistItem = { id: string; task_id: string; space_id: string; owner_id: string; label: string; done: boolean; sort_order: number };
export type RecurrenceRule = { id: string; rrule: string; dtstart: string; until: string | null; timezone: string; exdates: string[] };
export type Event = { id: string; space_id: string; owner_id: string; title: string; body_md: string; location_text: string | null; place_id: string | null; starts_at: string; ends_at: string; all_day: boolean; timezone: string; status: string; visibility: string; is_locked: boolean; recurrence_rule_id: string | null; recurrence_rules?: RecurrenceRule | null };
export type Person = { id: string; space_id: string; owner_id: string; display_name: string; given_name: string | null; family_name: string | null; nickname: string | null; pronouns: string | null; notes_md: string; is_locked: boolean; archived_at: string | null; home_place_id: string | null };
export type PersonContact = { id: string; person_id: string; kind: string; label: string; value: string; is_primary: boolean };
export type PersonDate = { id: string; person_id: string; kind: string; label: string | null; on_date: string; year_known: boolean; remind_days: number };
export type Place = { id: string; space_id: string; owner_id: string; name: string; address_text: string | null; postcode: string | null; city: string | null; country_code: string; notes_md: string; is_locked: boolean; archived_at: string | null; geom: unknown };
export type Note = { id: string; space_id: string; owner_id: string; title: string; body_md: string; visibility: string; is_locked: boolean; pinned_at: string | null; archived_at: string | null; updated_at: string };
export type SearchResult = { id: string; type: 'task' | 'note' | 'person' | 'event' | 'place'; title: string; subtitle: string; space_id: string; path: string };
